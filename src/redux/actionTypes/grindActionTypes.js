export const SET_GRID_VALUES_ARRAY = 'SET_GRID_VALUES_ARRAY'

// for now we have only one case but in real case maybe we'll get it
// from api request, so we need to handle this for success and error cases
